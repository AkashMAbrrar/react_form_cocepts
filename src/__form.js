/**
 * 0: e.preventDefault(): for default page reload
 * 1: use for tag and name to get values
 * 2: right onSubmit in the form for handle event
 * 3: take a parameter (e) in the handle event function and set prevent default first.
 * 4: take the value of this form by e.target.name.value
 * 5: we have write name for each form according to their action
 *
 *
 * 6: set individual state field for each input field
 *
 * 4: uncontrolled element: useRef to create a reference to the element and the access value by using like: nameRef.current.value
 * **/
